# st-projects
Just a few basic projects

## Run blog app (Termux)

1. `cd st-projects`
2. `node server.js`
3. Open `http://127.0.0.1:3000` in your phone browser.

If you want to access from another device on same network, use your phone IP:
`http://<phone-ip>:3000`
